/***
 *
 */
const expect = require('chai').expect;
const assert = require('assert');

/***
 *
 */
describe('DHIS2 Integration Mediator(DIM Mediator)', function() {
	/***
	 *
	 */
	describe('Main page', function() {
		/***
		 *
		 */
		it('It should return true if it execute successfully', async done => {
			/***
			 *
			 */
			await done()
			await assert.equal(true, true);
		});
	});
});
